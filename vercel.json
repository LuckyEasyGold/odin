{
  "version": 2,
  "projects": [
    {
      "outputDirectory": "apps/web/.next",
      "rootDirectory": "apps/web",
      "buildCommand": "pnpm --filter @odin/core build && pnpm --filter @odin/storage build && pnpm --filter @odin/engine build && pnpm build",
      "developCommand": "pnpm dev",
      "installCommand": "pnpm install --frozen-lockfile",
      "developmentConfiguration": {
        "framework": "nextjs"
      }
    },
    {
      "outputDirectory": "apps/api/dist",
      "rootDirectory": "apps/api",
      "buildCommand": "pnpm --filter @odin/core build && pnpm --filter @odin/storage build && pnpm --filter @odin/engine build && pnpm build",
      "developCommand": "pnpm dev",
      "installCommand": "pnpm install --frozen-lockfile",
      "developmentConfiguration": {
        "framework": "node"
      }
    }
  ]
}
    },
    {
      "outputDirectory": "apps/api/dist",
      "rootDirectory": "apps/api",
      "buildCommand": "cd ../../ && pnpm --filter @odin/core build && pnpm --filter @odin/storage build && pnpm --filter @odin/engine build && cd apps/api && pnpm build",
      "installCommand": "cd ../../ && pnpm install --frozen-lockfile && pnpm --filter @odin/core build && pnpm --filter @odin/storage build && pnpm --filter @odin/engine build && cd apps/api && pnpm install --frozen-lockfile",
      "developmentConfiguration": {
        "framework": "node"
      }
    }
  ]
}
